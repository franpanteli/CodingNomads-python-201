# Create a lambda expression that takes no input
# and prints "hello world" to the console. What does it return?
lambda_expression = lambda: print("Hello, World!")
lambda_expression() #it does not return "Hello, World!", it only prints it out to the console