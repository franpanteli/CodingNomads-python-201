"""
    -> CSV <- comma-separated values
    -> this is like an Excel format
    -> they only separate values with commas
    -> you don't need Python to write CSV files, but with the Python CSV module, you there is extra functionality
        -> headers, different flavors
        -> it keeps the syntax correct
    -> they are hard to produce manually (Python is better for this)
    -> JSON <- this is later in the course
        -> you can read the data into the .py file
        -> this is for transferring information over the internet
    -> summary
        -> context managers can be used with the `with` keywords, to open file objects
        -> the CSV module can be used to write data to a file
            -> this formats the file
"""