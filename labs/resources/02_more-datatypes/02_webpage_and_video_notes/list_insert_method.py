bucket_list = ["climb Mt. Everest", "eat fruits from a tree that I planted"] #creates list

# Add an element
bucket_list.insert(1, "sail across the Atlantic ocean") #inserts element at index 1, with the text in the second argument
    #to use a method, isn't the list_name.method
    #these methods are used on lists and not tuples, because one is immutable and the other is not
print(bucket_list)
