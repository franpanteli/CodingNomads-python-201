"""-> def <- to define a function
    -> give it a name that describes what the function will do
    -> def greet(greeting, name): <- greeting and name are arguments
        -> function body
            -> in an indentation
            -> sentence = f"{greeting}, {name}! How are you?"
            -> he defines a function and then uses the arguments in the text of the function call
            -> return sentence
                -> constructing the string and using the function to return it
    -> the function can then be called, by using the greet() method
        -> arguments can be passed into this
    """