courses = ['Intro', 'Intermediate', 'Advanced', 'Professional'] #a list of courses

for index in range(len(courses)): #you can iterate from 1 to the length of a list
    print(f"{index}: {courses[index]} Python") #you can also have an f string which prints these values

#this is an alternative to the enumerate() function
#enumerate() is a built-in function
